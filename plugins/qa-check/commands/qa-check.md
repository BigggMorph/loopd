---
description: PR-레벨 QA 에이전트 — diff+티켓 의도에서 L3(브라우저) QA 시나리오를 생성·실행하고 머지 판단 증거 리포트를 만든다.
argument-hint: "<PR번호 | 커밋 | 커밋범위>"
allowed-tools: [Bash, Read, Write, Edit, Glob, Grep]
---

Invoke the `qa-check` skill with `$ARGUMENTS`.

머지 판단은 사람이 한다. 이 에이전트의 일은 사람이 판단할 수 있는 증거를 만드는 것이다.
